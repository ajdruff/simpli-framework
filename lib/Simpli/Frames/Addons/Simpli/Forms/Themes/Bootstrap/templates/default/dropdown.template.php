<!-- Vertical with Resizing -->

<div class="form-group">
    <div class="row">
        <div class="col-{ds}-{label_size}">
            <label for="{ID}" >{LABEL}   </label>
        </div>
    </div>
    <div class="row">
        <div class="col-{ds}-{size}">
        <select name='{name}' style="{style}" class="form-control {class}" id="{ID}">

            {options_html}

        </select>
        </div>
    </div>

</div>

<!-- vertical form with sizing -->
<div class="form-group">
    <div class="row">
        <div class="col-{ds}-{label_size}">
            <label class="pull-left" for="{ID}" >{LABEL}   </label>
        </div>
    </div>
    <div class="row">
        <div class="col-{ds}-{size}">
            <textarea style="{STYLE}" name="{name}" class="form-control {CLASS}" id="{ID}" rows="{ROWS}" cols="{COLS}" placeholder="{PLACEHOLDER}" >{VALUE}</textarea>
        </div>
    </div>

</div>

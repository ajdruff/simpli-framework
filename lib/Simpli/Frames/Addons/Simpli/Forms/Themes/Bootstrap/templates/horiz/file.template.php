<div class="form-group">
    <div class="row">
        <div class="col-{ds}-{label_size}">
            <label   for="{ID}" >{LABEL}   </label>
        </div>
        <div class="col-{ds}-5">
            <input type="file" accept="{accept}" style="{STYLE}" name="{name}" class="{CLASS}" id="{ID}" placeholder="{PLACEHOLDER}" value="{VALUE}">
        </div>
    </div>
</div>
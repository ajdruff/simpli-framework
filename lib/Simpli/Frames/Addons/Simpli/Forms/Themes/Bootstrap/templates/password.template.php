<!-- vertical form with sizing -->
<div class="form-group">
    <div class="row">
        <div class="col-{ds}-{label_size}">
            <label for="{ID}" >{LABEL}   </label>
        </div>
    </div>
    <div class="row">
        <div class="col-{ds}-{size}">
            <input type="password" style="{STYLE}" name="{name}" class="form-control {CLASS}" id="{ID}" placeholder="{PLACEHOLDER}" value="{VALUE}">
        </div>
    </div>

</div>







